import React from 'react';
import Card from '../Card/Card'

class CardList extends React.Component {
    render () {
        return (
            <ul>
                {/* {this.props.key_points.map((key_point)=> {
                    return <Card key_point={key_point}/>
                })} */}
            </ul>
        )
    }
}

export default CardList;